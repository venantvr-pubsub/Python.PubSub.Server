# Chemin vers le fichier de base de données SQLite
# Utiliser ":memory:" pour une base de données en mémoire (volatile, par défaut)
# Ou spécifier un chemin de fichier pour la persistance (ex: pubsub.db)
DATABASE_FILE=:memory:

# Nombre maximum de lignes par table (messages, subscriptions, consumptions)
# Les plus anciennes sont automatiquement supprimées lorsque la limite est atteinte
# Utilisez 0 ou moins pour désactiver la limite
MAX_ROWS_PER_TABLE=5000
